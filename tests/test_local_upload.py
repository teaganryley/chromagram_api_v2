def test_local_upload():
    pass


def test_save():
    """
    -verify that good extensions will actually be saved to the file system
    read from config?
    do i need to make a test config?
    :return:
    """


def test_is_allowed():
    """
    -after constucting a legit object, do the following:
        -test that bad extensions get rejected
        -good extensions get passed through
        read extension type from config? if so, which config?

    :return:
    """